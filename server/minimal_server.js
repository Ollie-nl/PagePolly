console.log("Server minimal test"); const express = require("express"); const app = express(); const port = 3000; app.get("/", (req, res) => res.send("Test OK")); app.listen(port, () => console.log(`Test server listening on port ${port}`));
